export const BookCard = ({ title, imageUrl, categories, autors }) => {
    return (
        <div>
            <h1>{title}</h1>
            <h1>{imageUrl}</h1>
            <h1>{categories}</h1>
            <h1>{autors}</h1>

        </div>
    )

} 